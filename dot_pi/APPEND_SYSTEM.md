Always use `bun` instead of `node` for running scripts, installing packages, and project commands.

Put personal scripts and executables in `~/.local/bin/` (which is on PATH). Use subdirectories there for organizing scripts by topic (e.g. `~/.local/bin/niri/`).
